import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mxqin.stockdashboard',
  appName: 'Stock Dashboard',
  webDir: 'dist'
};

export default config;
